type: object
required:
  - email
  - password
properties:
  email:
    description: User's email address
    type: string
  password:
    description: User's password
    type: string
    format: password